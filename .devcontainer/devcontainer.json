{
  "name": "Python 3",
  "dockerComposeFile": "../docker-compose.yml",
  "service": "tf2mapchangenotifierbot",
  "workspaceFolder": "/app",
  "postCreateCommand": "pip3 install --user -r requirements.txt",
  "mounts": [ "source=sqlite-data,target=/app/db" ]
}